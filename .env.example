# Ключ берётся бесплатно на aistudio.google.com («Get API key», карта не нужна).
# Без ключа приложение работает по regex-fallback.
# AFINA_AI_MODEL опционально переопределяет модель (дефолт в коде — gemini-2.5-flash).
GOOGLE_GENERATIVE_AI_API_KEY=
AFINA_AI_MODEL=
